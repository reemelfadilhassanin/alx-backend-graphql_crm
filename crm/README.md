## Weekly CRM Report - Celery Configuration

### Prerequisites

- Redis server running on `localhost:6379`
- Installed dependencies: celery, django-celery-beat, redis

### Setup Instructions

```bash
pip install -r requirements.txt
python manage.py migrate
```
